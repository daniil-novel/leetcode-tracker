# Запуск Grafana через Docker Desktop GUI 🐳

## Шаг 1: Установка Docker Desktop

1. **Скачайте Docker Desktop для Windows:**
   - Перейдите на https://www.docker.com/products/docker-desktop
   - Нажмите "Download for Windows"
   - Запустите установщик `Docker Desktop Installer.exe`

2. **Установите Docker Desktop:**
   - Следуйте инструкциям установщика
   - При необходимости включите WSL 2 (Windows Subsystem for Linux)
   - Перезагрузите компьютер, если потребуется

3. **Запустите Docker Desktop:**
   - Найдите Docker Desktop в меню Пуск
   - Запустите приложение
   - Дождитесь, пока Docker полностью запустится (иконка в трее станет зеленой)

## Шаг 2: Запуск Grafana через Docker Desktop GUI

### Вариант A: Через Docker Compose (Рекомендуется)

1. **Откройте Docker Desktop**

2. **Перейдите в раздел "Containers"** (левое меню)

3. **Нажмите кнопку "Create" или "+" в правом верхнем углу**

4. **Выберите "From Docker Compose"**

5. **Укажите путь к файлу:**
   ```
   E:\leetcode_tracker_uv\docker-compose.yml
   ```

6. **Нажмите "Start" или "Run"**

7. **Готово!** Контейнер `leetcode-tracker-grafana` появится в списке

### Вариант B: Через командную строку в Docker Desktop

1. **Откройте Docker Desktop**

2. **Нажмите на иконку терминала** (CLI) в правом верхнем углу

3. **Выполните команду:**
   ```bash
   cd E:\leetcode_tracker_uv
   docker-compose up -d
   ```

4. **Контейнер запустится автоматически**

### Вариант C: Через встроенный терминал Windows

1. **Откройте PowerShell или CMD**

2. **Перейдите в директорию проекта:**
   ```bash
   cd E:\leetcode_tracker_uv
   ```

3. **Запустите Docker Compose:**
   ```bash
   docker-compose up -d
   ```

4. **Откройте Docker Desktop** - контейнер появится в списке

## Шаг 3: Проверка запуска в Docker Desktop

После запуска в Docker Desktop вы увидите:

1. **В разделе "Containers":**
   - Контейнер `leetcode-tracker-grafana`
   - Статус: **Running** (зеленый индикатор)
   - Порт: `3000:3000`

2. **Действия с контейнером:**
   - **▶️ Start** - запустить контейнер
   - **⏸️ Stop** - остановить контейнер
   - **🔄 Restart** - перезапустить контейнер
   - **🗑️ Delete** - удалить контейнер
   - **📋 Logs** - посмотреть логи
   - **⚙️ Inspect** - детальная информация
   - **🖥️ CLI** - открыть терминал внутри контейнера

## Шаг 4: Открытие Grafana

### Способ 1: Через Docker Desktop

1. В списке контейнеров найдите `leetcode-tracker-grafana`
2. Нажмите на контейнер
3. В правой панели найдите секцию **"Ports"**
4. Нажмите на ссылку `3000:3000` или кнопку **"Open in browser"**
5. Откроется http://localhost:3000

### Способ 2: Вручную

1. Откройте браузер
2. Перейдите на http://localhost:3000
3. Войдите:
   - Username: `admin`
   - Password: `admin`

## Шаг 5: Управление контейнером

### Остановить Grafana:

**Через Docker Desktop:**
1. Найдите контейнер `leetcode-tracker-grafana`
2. Нажмите кнопку **Stop** (⏸️)

**Через командную строку:**
```bash
docker-compose stop
```

### Запустить снова:

**Через Docker Desktop:**
1. Найдите контейнер `leetcode-tracker-grafana`
2. Нажмите кнопку **Start** (▶️)

**Через командную строку:**
```bash
docker-compose start
```

### Перезапустить:

**Через Docker Desktop:**
1. Найдите контейнер `leetcode-tracker-grafana`
2. Нажмите кнопку **Restart** (🔄)

**Через командную строку:**
```bash
docker-compose restart
```

### Посмотреть логи:

**Через Docker Desktop:**
1. Найдите контейнер `leetcode-tracker-grafana`
2. Нажмите на контейнер
3. Перейдите на вкладку **"Logs"**

**Через командную строку:**
```bash
docker-compose logs -f grafana
```

### Удалить контейнер:

**Через Docker Desktop:**
1. Остановите контейнер (Stop)
2. Нажмите кнопку **Delete** (🗑️)

**Через командную строку:**
```bash
docker-compose down
```

## Troubleshooting

### Docker Desktop не запускается

1. **Проверьте WSL 2:**
   - Откройте PowerShell от имени администратора
   - Выполните: `wsl --install`
   - Перезагрузите компьютер

2. **Проверьте виртуализацию:**
   - Откройте Task Manager (Ctrl+Shift+Esc)
   - Вкладка "Performance" → "CPU"
   - Убедитесь, что "Virtualization: Enabled"

3. **Переустановите Docker Desktop:**
   - Удалите Docker Desktop
   - Скачайте последнюю версию
   - Установите заново

### Контейнер не запускается

1. **Проверьте логи в Docker Desktop:**
   - Нажмите на контейнер
   - Вкладка "Logs"
   - Посмотрите ошибки

2. **Проверьте порт 3000:**
   - Убедитесь, что порт 3000 не занят другим приложением
   - В PowerShell: `netstat -ano | findstr :3000`

3. **Пересоздайте контейнер:**
   ```bash
   docker-compose down
   docker-compose up -d
   ```

### Grafana не открывается в браузере

1. **Проверьте статус контейнера:**
   - В Docker Desktop контейнер должен быть **Running**

2. **Подождите 10-20 секунд:**
   - Grafana может загружаться при первом запуске

3. **Попробуйте другой браузер:**
   - Chrome, Firefox, Edge

4. **Очистите кэш браузера:**
   - Ctrl+Shift+Delete

## Полезные команды Docker Desktop

### Просмотр всех контейнеров:
```bash
docker ps -a
```

### Просмотр образов:
```bash
docker images
```

### Очистка неиспользуемых ресурсов:
```bash
docker system prune
```

### Обновление образа Grafana:
```bash
docker-compose pull
docker-compose up -d
```

## Автозапуск при старте Windows

1. **Откройте Docker Desktop Settings** (⚙️)
2. **General** → включите **"Start Docker Desktop when you log in"**
3. **Resources** → настройте выделенные ресурсы (CPU, Memory)

## Резервное копирование данных Grafana

Данные Grafana хранятся в `grafana/data/`. Для бэкапа:

1. **Остановите контейнер**
2. **Скопируйте папку:**
   ```bash
   xcopy /E /I grafana\data grafana\data_backup
   ```
3. **Запустите контейнер снова**

---

**Готово!** Теперь вы можете управлять Grafana через удобный GUI Docker Desktop 🎉
