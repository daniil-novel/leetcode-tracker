# Автоматическая синхронизация с LeetCode

## Обзор

Приложение теперь имеет **автоматическую фоновую синхронизацию** с LeetCode, которая работает постоянно в фоновом режиме и обновляет данные каждые 10 секунд.

## Как это работает

### Автоматический режим

1. **При запуске приложения** автоматически стартует фоновый сервис синхронизации
2. **Каждые 10 секунд** сервис проверяет всех пользователей с установленным LeetCode username
3. **Для каждого пользователя** загружаются последние 20 принятых решений
4. **Новые задачи** автоматически добавляются в базу данных
5. **Задачи сразу отображаются** в календаре и статистике

### Что синхронизируется

Для каждого решения сохраняется:
- ✅ **Название задачи** - точное название с LeetCode
- ✅ **Дата решения** - когда задача была решена
- ✅ **Сложность** - Easy/Medium/Hard (определяется автоматически)
- ✅ **XP** - рассчитывается по сложности (Easy: 1, Medium: 3, Hard: 5)
- ✅ **Язык программирования** - сохраняется в заметках
- ✅ **ID задачи** - уникальный идентификатор

## Настройка

### Шаг 1: Установите LeetCode Username

Используйте API эндпоинт:

```bash
curl -X PUT "https://novel-cloudtech.com:7443/api/sync/leetcode-username?leetcode_username=ваш_username" \
  -H "Authorization: Bearer ВАШ_JWT_TOKEN"
```

Или через Swagger UI: https://novel-cloudtech.com:7443/docs

### Шаг 2: Готово!

После установки username синхронизация начнётся автоматически. Новые решения будут появляться в календаре в течение 10 секунд после их принятия на LeetCode.

## Конфигурация

### Переменные окружения (.env)

```bash
# Включить/выключить автоматическую синхронизацию
LEETCODE_SYNC_ENABLED=true

# Интервал синхронизации в секундах (по умолчанию 10)
LEETCODE_SYNC_INTERVAL=10
```

### Изменение интервала

Чтобы изменить частоту синхронизации, добавьте в `.env`:

```bash
# Синхронизация каждые 5 секунд
LEETCODE_SYNC_INTERVAL=5

# Синхронизация каждые 30 секунд
LEETCODE_SYNC_INTERVAL=30

# Синхронизация каждую минуту
LEETCODE_SYNC_INTERVAL=60
```

### Отключение автосинхронизации

Если нужно отключить автоматическую синхронизацию:

```bash
LEETCODE_SYNC_ENABLED=false
```

## Архитектура

### Фоновый сервис

```python
class LeetCodeSyncService:
    """Background service for continuous LeetCode synchronization"""
    
    async def _sync_loop(self):
        """Main sync loop"""
        while self.is_running:
            # Синхронизация всех пользователей
            await self._sync_all_users()
            
            # Ожидание следующего цикла
            await asyncio.sleep(self.sync_interval)
```

### Жизненный цикл

1. **Startup** - сервис запускается при старте приложения
2. **Running** - работает в фоновом режиме
3. **Shutdown** - корректно останавливается при остановке приложения

### Логирование

Все операции синхронизации логируются:

```
INFO: 🔄 LeetCode sync service started (interval: 10s)
INFO: Syncing 3 users...
INFO: ✅ Synced 2 new tasks for user john_doe
INFO: ✅ Sync completed for 3 users
```

## Преимущества автосинхронизации

### ✅ Полностью автоматическая

- Не нужно вручную запускать синхронизацию
- Не нужно нажимать кнопки
- Работает 24/7 в фоновом режиме

### ✅ Мгновенное обновление

- Новые решения появляются в течение 10 секунд
- Данные всегда актуальны
- Не нужно обновлять страницу

### ✅ Эффективная

- Синхронизирует только новые задачи
- Предотвращает дубликаты
- Минимальная нагрузка на LeetCode API

### ✅ Надёжная

- Обработка ошибок для каждого пользователя
- Продолжает работу при ошибках
- Автоматический retry

## Мониторинг

### Проверка статуса

```bash
curl "https://novel-cloudtech.com:7443/api/sync/status" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Ответ:
```json
{
  "leetcode_username": "your_username",
  "has_leetcode_username": true,
  "total_leetcode_tasks": 150
}
```

### Логи сервера

Проверьте логи для мониторинга синхронизации:

```bash
# На сервере
journalctl -u leetcode-tracker -f
```

Вы увидите:
```
INFO: 🔄 LeetCode sync service started (interval: 10s)
INFO: Syncing 1 users...
INFO: ✅ Synced 1 new tasks for user username
INFO: ✅ Sync completed for 1 users
```

## Производительность

### Оптимизация

- **Лимит запросов**: Загружается только 20 последних решений за раз
- **Кэширование сложности**: Сложность задачи кэшируется в рамках одного цикла
- **Проверка дубликатов**: Быстрая проверка существующих задач в БД
- **Асинхронность**: Все операции асинхронные, не блокируют приложение

### Нагрузка на LeetCode API

При интервале 10 секунд и 1 пользователе:
- **6 запросов в минуту** (1 запрос каждые 10 секунд)
- **360 запросов в час**
- **8,640 запросов в день**

Это безопасно для LeetCode API, так как:
- Запросы распределены во времени
- Используется официальный GraphQL API
- Нет массовых запросов

## Troubleshooting

### Синхронизация не работает

**Проверьте:**
1. LeetCode username установлен: `GET /api/sync/status`
2. Сервис запущен: проверьте логи
3. Профиль на LeetCode публичный
4. `LEETCODE_SYNC_ENABLED=true` в `.env`

### Задачи не появляются

**Возможные причины:**
1. Задачи уже существуют в БД (дубликаты не добавляются)
2. Нет новых принятых решений на LeetCode
3. Ошибка при получении данных (проверьте логи)

### Слишком частая синхронизация

**Решение:**
Увеличьте интервал в `.env`:
```bash
LEETCODE_SYNC_INTERVAL=30  # 30 секунд вместо 10
```

### Ошибки в логах

**Типичные ошибки:**

1. **"LeetCode user not found"**
   - Проверьте правильность username
   - Убедитесь, что профиль публичный

2. **"Could not fetch difficulty"**
   - Временная проблема с LeetCode API
   - Задача будет добавлена со сложностью "Medium"

3. **"Error syncing user"**
   - Проверьте подключение к интернету
   - Проверьте доступность LeetCode

## Сравнение с ручной синхронизацией

| Функция | Автосинхронизация | Ручная синхронизация |
|---------|-------------------|----------------------|
| Запуск | Автоматически | Вручную через API |
| Частота | Каждые 10 секунд | По требованию |
| Актуальность | Всегда свежие данные | Зависит от пользователя |
| Удобство | Максимальное | Требует действий |
| Нагрузка | Постоянная низкая | Пиковая при запуске |

## Рекомендации

### Для разработки

```bash
# Более редкая синхронизация для экономии запросов
LEETCODE_SYNC_INTERVAL=60
```

### Для продакшена

```bash
# Быстрая синхронизация для актуальности данных
LEETCODE_SYNC_INTERVAL=10
```

### Для большого количества пользователей

```bash
# Увеличьте интервал чтобы не перегружать LeetCode API
LEETCODE_SYNC_INTERVAL=30
```

## Безопасность

- ✅ Синхронизация работает только для авторизованных пользователей
- ✅ Каждый пользователь видит только свои задачи
- ✅ Используется официальный LeetCode API
- ✅ Нет хранения паролей LeetCode
- ✅ Только публичные данные

## Заключение

Автоматическая синхронизация делает использование приложения максимально удобным:

1. **Установите LeetCode username один раз**
2. **Решайте задачи на LeetCode**
3. **Задачи автоматически появляются в вашем трекере**

Никаких дополнительных действий не требуется! 🎉

## Поддержка

Для вопросов и проблем:
- Проверьте логи: `journalctl -u leetcode-tracker -f`
- API документация: https://novel-cloudtech.com:7443/docs
- Проверьте статус: `GET /api/sync/status`
