# 🔐 Настройка GitHub OAuth для LeetCode Tracker

## ✅ Текущее состояние

Сервер настроен и работает на: `http://v353999.hosted-by-vdsina.com:8000`

Аутентификация: **только через GitHub OAuth** (простая авторизация отключена)

---

## 📋 Что нужно сделать

### Шаг 1: Настроить GitHub OAuth App

1. Перейдите на: https://github.com/settings/developers

2. Если OAuth App уже создано:
   - Найдите приложение "LeetCode Tracker"
   - Нажмите **"Edit"**

3. Если OAuth App НЕ создано:
   - Нажмите **"New OAuth App"**

4. Заполните поля:
   ```
   Application name: LeetCode Tracker
   Homepage URL: http://v353999.hosted-by-vdsina.com:8000
   Authorization callback URL: http://v353999.hosted-by-vdsina.com:8000/auth/callback/github
   ```

5. Нажмите **"Register application"** (или "Update application")

### Шаг 2: Проверить Client ID и Secret

На странице вашего OAuth App проверьте:

- **Client ID**: должен быть `Ov23lijCFWyjFX6T5fHk` (уже настроен в `.env`)
- **Client Secret**: если нужно, сгенерируйте новый и обновите в `.env`

---

## 🧪 Как протестировать

1. Откройте в браузере: http://v353999.hosted-by-vdsina.com:8000/login

2. Вы должны увидеть страницу с одной кнопкой **"Войти через GitHub"**

3. Нажмите кнопку → вас перенаправит на GitHub

4. Авторизуйте приложение → вас вернет обратно, и вы войдете в систему

---

## ⚠️ Возможные проблемы и решения

### Проблема 1: "Redirect URI mismatch"
**Решение**: Проверьте, что в настройках GitHub OAuth App указан точный URL:
```
http://v353999.hosted-by-vdsina.com:8000/auth/callback/github
```

### Проблема 2: "Application not found"
**Решение**: Убедитесь, что Client ID в `.env` совпадает с Client ID в GitHub App

### Проблема 3: После авторизации ошибка
**Решение**: Проверьте логи сервера:
```bash
ssh root@91.84.104.36
tail -50 /root/leetcode_tracker_uv/leetcode_tracker_uv/server.log
```

---

## 📁 Файлы на сервере

Все файлы находятся в: `/root/leetcode_tracker_uv/leetcode_tracker_uv/`

- `.env` - переменные окружения (GitHub Client ID/Secret)
- `leetcode_tracker/templates/login.html` - страница входа
- `leetcode_tracker/main.py` - эндпоинты OAuth
- `leetcode_tracker/auth.py` - логика аутентификации

---

## 🔧 Перезапуск сервера

Если нужно перезапустить сервер:

```bash
ssh root@91.84.104.36
cd /root/leetcode_tracker_uv/leetcode_tracker_uv
fuser -k 8000/tcp
nohup /root/.local/bin/uv run uvicorn leetcode_tracker.main:app --host 0.0.0.0 --port 8000 > server.log 2>&1 &
```

Или используйте локальный скрипт:
```bash
python ssh_restart_server.py
```

---

## ✅ Готово!

После правильной настройки GitHub OAuth App аутентификация будет работать полностью.

Проверьте: http://v353999.hosted-by-vdsina.com:8000
