# 📊 Инструкция по решению проблемы с данными в Grafana

## 🎯 Проблема

Grafana показывает "No data" - либо не видит данных в базе, либо их там нет.

## ✅ Быстрое решение

### 1. Диагностика проблемы

Сначала проверим, что не так на сервере:

```bash
ssh root@v353999.hosted-by-vdsina.com
cd /root/leetcode_tracker_uv
bash check_grafana_data.sh
```

Этот скрипт покажет:
- Работают ли контейнеры
- Подключена ли база данных PostgreSQL
- Сколько пользователей и задач в базе
- Настроена ли синхронизация с LeetCode

### 2. Настройка синхронизации данных

Данные берутся из LeetCode API автоматически каждые 10 секунд. Для этого нужно:

**На сервере выполните:**

```bash
cd /root/leetcode_tracker_uv
bash test_leetcode_sync.sh
```

Скрипт попросит ввести LeetCode username (например: `admin-daniil`) и:
- Обновит настройки пользователя
- Перезапустит синхронизацию
- Покажет результаты

### 3. Проверка результата

Через 15-20 секунд после запуска синхронизации:

1. Откройте Grafana: https://novel-cloudtech.com:7443/grafana/
2. Логин: `admin`, Пароль: `admin`
3. Откройте дашборд "LeetCode Tracker Dashboard"
4. Данные должны появиться

## 🔧 Альтернативный метод (если API не работает)

Если синхронизация через API не работает, добавьте тестовые данные вручную:

```bash
ssh root@v353999.hosted-by-vdsina.com
cd /root/leetcode_tracker_uv

# Добавить тестовые задачи
docker-compose exec -T db psql -U leetcode_user -d leetcode_tracker -c "
INSERT INTO solved_tasks (user_id, date, title, difficulty, points, platform) VALUES
  (1, CURRENT_DATE, 'Two Sum', 'Easy', 1, 'leetcode'),
  (1, CURRENT_DATE, 'Add Two Numbers', 'Medium', 3, 'leetcode'),
  (1, CURRENT_DATE - INTERVAL '1 day', 'Longest Substring', 'Medium', 3, 'leetcode'),
  (1, CURRENT_DATE - INTERVAL '2 days', 'Median of Two Sorted Arrays', 'Hard', 5, 'leetcode');
"
```

## 🚀 Деплой изменений на сервер

Я создал 3 новых скрипта для диагностики и деплоя:

### Новые файлы:

1. **check_grafana_data.sh** - диагностика данных и подключения
2. **test_leetcode_sync.sh** - настройка и тест синхронизации
3. **deploy_to_vdsina.sh** - автоматический деплой на сервер
4. **GRAFANA_DATA_FIX.md** - подробная документация (на английском)

### Как задеплоить:

**Вариант 1: Через Git (рекомендуется)**

```bash
# Локально
git add .
git commit -m "Add Grafana data diagnostic and sync scripts"
git push origin main

# На сервере
ssh root@v353999.hosted-by-vdsina.com
cd /root/leetcode_tracker_uv
git pull origin main
```

**Вариант 2: Автоматический деплой через скрипт**

```bash
# Локально (в директории проекта)
bash deploy_to_vdsina.sh
```

Этот скрипт:
- Закоммитит изменения
- Отправит в GitHub
- Подключится к серверу
- Обновит код
- Запустит деплой

## 📋 Чеклист проверки после деплоя

На сервере выполните:

```bash
ssh root@v353999.hosted-by-vdsina.com
cd /root/leetcode_tracker_uv

# 1. Проверка состояния
bash check_grafana_data.sh

# 2. Настройка синхронизации (если нужно)
bash test_leetcode_sync.sh

# 3. Проверка логов
docker-compose logs -f app | grep sync
```

## 🔍 Проверка подключения Grafana к базе

Настройки Grafana хранятся в [`grafana/provisioning/datasources/postgres.yml`](grafana/provisioning/datasources/postgres.yml:1):

```yaml
datasources:
  - name: LeetCode Tracker PostgreSQL
    type: postgres
    url: db:5432                    # ✅ Правильный хост
    database: leetcode_tracker      # ✅ Правильная база
    user: leetcode_user             # ✅ Правильный пользователь
```

Это уже настроено корректно в вашем проекте!

## 🎓 Как работает синхронизация

1. **Автоматическая синхронизация** ([`leetcode_tracker/background_sync.py`](leetcode_tracker/background_sync.py:1))
   - Запускается при старте приложения
   - Работает каждые 10 секунд (настраивается)
   - Получает данные из LeetCode API

2. **Настройки** ([`leetcode_tracker/config.py`](leetcode_tracker/config.py:1))
   ```python
   leetcode_sync_enabled: bool = True      # Включить/выключить
   leetcode_sync_interval: int = 10        # Интервал в секундах
   ```

3. **Требования**
   - У пользователя должен быть указан `leetcode_username`
   - LeetCode API должен быть доступен
   - Профиль LeetCode должен быть публичным

## ⚠️ Типичные ошибки

### "No data" в Grafana
✅ **Решение**: Настройте синхронизацию через `bash test_leetcode_sync.sh`

### "Connection refused" в Grafana
✅ **Решение**: 
```bash
docker-compose restart db grafana
```

### Синхронизация не работает
✅ **Решение**: Проверьте логи
```bash
docker-compose logs app | grep -i "sync\|error"
```

## 📞 Дополнительная помощь

Полная документация: [`GRAFANA_DATA_FIX.md`](GRAFANA_DATA_FIX.md:1)

**Важные файлы:**
- [`docker-compose.yml`](docker-compose.yml:1) - конфигурация Docker
- [`leetcode_tracker/models.py`](leetcode_tracker/models.py:1) - структура БД
- [`grafana/provisioning/`](grafana/provisioning/) - настройки Grafana
- [`deploy.sh`](deploy.sh:1) - скрипт деплоя на сервере
