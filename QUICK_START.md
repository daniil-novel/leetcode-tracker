# 🚀 Быстрый старт LeetCode Tracker

## Супер-простая авторизация!

Теперь для входа нужно только **ввести ваше имя** - никаких паролей!

## Как запустить локально:

1. **Установите зависимости:**
```bash
uv sync
```

2. **ВАЖНО! Если у вас старая база данных, сбросьте её:**
```bash
python reset_database.py
```
Это удалит старую БД и создаст новую с правильной схемой.

3. **Запустите приложение:**
```bash
uv run uvicorn leetcode_tracker.main:app --reload
```

4. **Откройте в браузере:**
```
http://localhost:8000
```

4. **Войдите:**
- Введите любое имя (минимум 2 символа)
- Нажмите "Войти"
- Готово! 🎉

## Как запустить на сервере:

1. **Создайте `.env` файл:**
```bash
SECRET_KEY=your-random-secret-key-here
```

2. **Запустите:**
```bash
uv run uvicorn leetcode_tracker.main:app --host 0.0.0.0 --port 8000
```

Или в фоновом режиме:
```bash
nohup uv run uvicorn leetcode_tracker.main:app --host 0.0.0.0 --port 8000 > app.log 2>&1 &
```

3. **Откройте:**
```
http://ваш_сервер:8000
```

## Как это работает:

1. ✅ **Вход** - просто введите имя
2. ✅ **Автосоздание** - если пользователь новый, создается автоматически
3. ✅ **JWT токен** - сохраняется в localStorage на 7 дней
4. ✅ **Автоматическая авторизация** - токен автоматически добавляется ко всем запросам
5. ✅ **Приватность** - каждый пользователь видит только свои задачи

## Особенности:

- 🔐 **Без паролей** - максимально просто
- 🚀 **Быстрый вход** - за 2 секунды
- 📊 **Персональный трекинг** - ваши данные только для вас
- 🏆 **Лидерборд** - соревнуйтесь с друзьями
- 📈 **Графики** - визуализация прогресса

## Примеры использования:

### Локально для себя:
```bash
uv run uvicorn leetcode_tracker.main:app --reload
# Войдите как "MyName"
```

### На сервере для команды:
```bash
# На сервере
uv run uvicorn leetcode_tracker.main:app --host 0.0.0.0 --port 8000

# Каждый член команды входит со своим именем
# Пример: "Alex", "Maria", "John"
```

## Безопасность:

- JWT токены с истечением через 7 дней
- Каждый пользователь видит только свои задачи
- Настройки приватности для публичного профиля

## Поддержка:

Если что-то не работает - проверьте:
1. ✅ База данных создана (автоматически при первом запуске)
2. ✅ Порт 8000 свободен
3. ✅ uv установлен

Приятного использования! 🎉
