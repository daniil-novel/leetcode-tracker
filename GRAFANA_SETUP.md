# Grafana Setup для LeetCode Tracker 📊

## Быстрый старт

### 1. Установите Docker Desktop

Скачайте и установите Docker Desktop для Windows:
- Перейдите на https://www.docker.com/products/docker-desktop
- Скачайте Docker Desktop для Windows
- Установите и запустите Docker Desktop
- Дождитесь полного запуска (иконка Docker в трее должна быть зеленой)

**📖 Подробная инструкция по работе с Docker Desktop GUI:** [GRAFANA_DOCKER_DESKTOP.md](GRAFANA_DOCKER_DESKTOP.md)

### 2. Запустите Grafana

**Вариант A: Через командную строку**

После установки и запуска Docker Desktop выполните:

```bash
docker-compose up -d
```

**Вариант B: Через Docker Desktop GUI**

1. Откройте Docker Desktop
2. Перейдите в "Containers"
3. Нажмите "Create" → "From Docker Compose"
4. Укажите путь: `E:\leetcode_tracker_uv\docker-compose.yml`
5. Нажмите "Start"

Подробнее в [GRAFANA_DOCKER_DESKTOP.md](GRAFANA_DOCKER_DESKTOP.md)

Эта команда:
- Скачает образ Grafana (при первом запуске)
- Установит плагин для работы с SQLite
- Запустит Grafana на порту 3000
- Автоматически настроит подключение к вашей базе данных

### 3. Откройте Grafana

Откройте браузер и перейдите на:
```
http://localhost:3000
```

**Данные для входа:**
- Username: `admin`
- Password: `admin`

При первом входе Grafana может попросить сменить пароль (можно пропустить).

### 4. Готово! 🎉

Dashboard "LeetCode Tracker Dashboard" будет автоматически доступен на главной странице.

## Что включено в Dashboard

### Метрики (верхняя панель):
1. **Total Tasks Solved** - общее количество решенных задач
2. **Total XP** - суммарный опыт (Easy=1, Medium=3, Hard=5)
3. **Active Users** - количество активных пользователей
4. **Avg Time per Task** - среднее время на задачу (в минутах)

### Графики:
1. **Tasks Solved Over Time** - график решенных задач по времени
2. **Tasks by Difficulty** - круговая диаграмма по сложности
3. **Top Users Leaderboard** - таблица лидеров по XP
4. **Recent Tasks** - последние 20 решенных задач

## Управление Grafana

### Остановить Grafana:
```bash
docker-compose stop
```

### Запустить снова:
```bash
docker-compose start
```

### Полностью удалить (с данными):
```bash
docker-compose down -v
```

### Посмотреть логи:
```bash
docker-compose logs -f grafana
```

### Перезапустить:
```bash
docker-compose restart
```

## Настройка Dashboard

Dashboard автоматически обновляется каждые 5 секунд. Вы можете:

1. **Изменить период обновления** - в правом верхнем углу
2. **Изменить временной диапазон** - выбрать "Last 7 days", "Last 30 days" и т.д.
3. **Редактировать панели** - нажать на название панели → Edit
4. **Добавить новые панели** - кнопка "Add panel" в верхнем меню

## Структура файлов

```
grafana/
├── provisioning/
│   ├── datasources/
│   │   └── sqlite.yml          # Настройка подключения к SQLite
│   └── dashboards/
│       ├── dashboard.yml       # Конфигурация провижининга
│       └── leetcode-tracker.json  # JSON дашборда
└── data/                       # Данные Grafana (в .gitignore)
```

## Доступ к данным

Grafana подключается к вашей базе данных `leetcode.db` в режиме **только для чтения** (`:ro`), поэтому:
- ✅ Безопасно - Grafana не может изменить ваши данные
- ✅ Актуально - показывает данные в реальном времени
- ✅ Независимо - работает параллельно с основным приложением

## Кастомизация

### Изменить пароль администратора

Отредактируйте `docker-compose.yml`:
```yaml
environment:
  - GF_SECURITY_ADMIN_USER=admin
  - GF_SECURITY_ADMIN_PASSWORD=your_secure_password
```

Затем перезапустите:
```bash
docker-compose down
docker-compose up -d
```

### Добавить свои запросы

1. Откройте dashboard
2. Нажмите на панель → Edit
3. В разделе Query измените SQL запрос
4. Сохраните изменения

### Примеры полезных запросов

**Задачи за последние 7 дней:**
```sql
SELECT date, COUNT(*) as count 
FROM tasks 
WHERE date >= date('now', '-7 days')
GROUP BY date
```

**Топ-5 самых активных пользователей:**
```sql
SELECT u.username, COUNT(t.id) as tasks
FROM users u
LEFT JOIN tasks t ON u.id = t.user_id
GROUP BY u.id
ORDER BY tasks DESC
LIMIT 5
```

**Распределение по времени решения:**
```sql
SELECT 
  CASE 
    WHEN time_spent < 30 THEN '< 30 min'
    WHEN time_spent < 60 THEN '30-60 min'
    ELSE '> 60 min'
  END as time_range,
  COUNT(*) as count
FROM tasks
WHERE time_spent IS NOT NULL
GROUP BY time_range
```

## Troubleshooting

### Grafana не запускается

1. Убедитесь, что Docker Desktop запущен
2. Проверьте, что порт 3000 свободен:
   ```bash
   netstat -ano | findstr :3000
   ```
3. Посмотрите логи:
   ```bash
   docker-compose logs grafana
   ```

### Нет данных в графиках

1. Убедитесь, что файл `leetcode.db` существует
2. Проверьте, что в базе есть данные:
   ```bash
   sqlite3 leetcode.db "SELECT COUNT(*) FROM tasks"
   ```
3. Проверьте подключение datasource в Grafana:
   - Configuration → Data sources → LeetCode Tracker SQLite
   - Нажмите "Test" - должно быть "Data source is working"

### Плагин SQLite не установлен

Если видите ошибку про плагин:
```bash
docker-compose down
docker-compose up -d
```

Grafana автоматически установит плагин при запуске.

## Дополнительные возможности

### Алерты (уведомления)

Вы можете настроить уведомления, например:
- Когда пользователь решил 100 задач
- Когда среднее время решения превысило порог
- Когда нет активности более 3 дней

### Экспорт дашборда

1. Settings (шестеренка) → JSON Model
2. Скопируйте JSON
3. Сохраните в файл для бэкапа

### Импорт дашборда

1. Dashboards → Import
2. Вставьте JSON или загрузите файл
3. Выберите datasource

## Полезные ссылки

- [Grafana Documentation](https://grafana.com/docs/)
- [SQLite Plugin](https://github.com/fr-ser/grafana-sqlite-datasource)
- [Dashboard Best Practices](https://grafana.com/docs/grafana/latest/dashboards/build-dashboards/best-practices/)

---

**Важно:** Grafana работает независимо от основного приложения. Вы можете запускать и останавливать их отдельно.
