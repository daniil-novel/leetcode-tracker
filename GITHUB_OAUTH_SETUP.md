# 🚀 Настройка входа через GitHub

Чтобы вход на сайте заработал, нужно получить ключи доступа от GitHub. Это просто и займет 2 минуты.

## Шаг 1: Создайте приложение на GitHub

1. Перейдите по ссылке: [https://github.com/settings/developers](https://github.com/settings/developers)
2. Нажмите зеленую кнопку **"New OAuth App"** (вверху справа).

## Шаг 2: Заполните форму

Заполните поля точно так, как указано ниже (данные для вашего сервера `v353999.hosted-by-vdsina.com`):

*   **Application name**: `LeetCode Tracker`
*   **Homepage URL**: `https://v353999.hosted-by-vdsina.com:7443`
*   **Application description**: (можно оставить пустым)
*   **Authorization callback URL**: `https://v353999.hosted-by-vdsina.com:7443/auth/callback/github`

👉 Нажмите кнопку **"Register application"**.

## Шаг 3: Получите ключи (Client ID и Client Secret)

После создания приложения вы попадете на страницу настроек.

1.  Найдите поле **Client ID**. Оно выглядит как набор букв и цифр (например: `Ov23li...`).
    *   Скопируйте его.
2.  Найдите раздел "Client secrets" и нажмите кнопку **"Generate a new client secret"**.
    *   Скопируйте появившийся длинный код (это ваш секретный ключ).

## Шаг 4: Добавьте ключи в проект

1.  В папке вашего проекта найдите файл `.env` (если его нет, скопируйте `.env.example` и переименуйте в `.env`).
2.  Откройте `.env` и вставьте ваши ключи:

```env
GITHUB_CLIENT_ID=сюда_вставьте_ваш_Client_ID
GITHUB_CLIENT_SECRET=сюда_вставьте_ваш_Client_Secret
SECRET_KEY=придумайте_любой_длинный_пароль
```

## Шаг 5: Перезапустите сервер

Если сервер был запущен, остановите его (Ctrl+C) и запустите снова:

```bash
uv run uvicorn leetcode_tracker.main:app --reload --host 0.0.0.0 --port 8000
```

---

Теперь вход через GitHub должен работать! 🎉
