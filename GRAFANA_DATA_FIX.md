# 🔧 Решение проблемы "No data" в Grafana

## 📋 Описание проблемы

Grafana показывает "No data", хотя:
- База данных PostgreSQL подключена
- Дашборд настроен правильно
- Данные должны синхронизироваться из LeetCode API

## 🔍 Причины проблемы

1. **Нет данных в базе** - таблица `solved_tasks` пуста
2. **Не настроена синхронизация** - у пользователей не указан `leetcode_username`
3. **Синхронизация не работает** - фоновый сервис не запущен или падает с ошибками

## ✅ Решение

### Шаг 1: Проверка текущего состояния

Используйте скрипт диагностики:

```bash
bash check_grafana_data.sh
```

Этот скрипт проверит:
- ✓ Статус контейнеров
- ✓ Подключение к PostgreSQL
- ✓ Наличие таблиц
- ✓ Количество пользователей и задач
- ✓ Настройки синхронизации
- ✓ Логи приложения

### Шаг 2: Настройка синхронизации

#### Вариант A: Автоматическая синхронизация через LeetCode API

1. Убедитесь, что у пользователя указан LeetCode username
2. Запустите тестовый скрипт:

```bash
bash test_leetcode_sync.sh
```

Или вручную через SQL:

```sql
-- Обновить leetcode_username для пользователя
docker-compose exec -T db psql -U leetcode_user -d leetcode_tracker -c \
  "UPDATE users SET leetcode_username = 'ваш_leetcode_username' WHERE id = 1;"

-- Перезапустить приложение для запуска синхронизации
docker-compose restart app
```

#### Вариант B: Ручное добавление данных

Если API синхронизация не работает, добавьте тестовые данные:

```sql
-- Вставить тестовые задачи
docker-compose exec -T db psql -U leetcode_user -d leetcode_tracker -c "
INSERT INTO solved_tasks (user_id, date, title, difficulty, points, platform) VALUES
  (1, '2024-12-16', 'Two Sum', 'Easy', 1, 'leetcode'),
  (1, '2024-12-16', 'Add Two Numbers', 'Medium', 3, 'leetcode'),
  (1, '2024-12-15', 'Longest Substring', 'Medium', 3, 'leetcode'),
  (1, '2024-12-14', 'Median of Two Sorted Arrays', 'Hard', 5, 'leetcode');
"
```

### Шаг 3: Проверка Grafana

1. Откройте Grafana: https://novel-cloudtech.com:7443/grafana/
2. Логин: `admin`, Пароль: `admin`
3. Перейдите в Dashboard "LeetCode Tracker"
4. Если данные не отображаются:
   - Нажмите на название панели → Edit
   - Внизу нажмите "Query Inspector" → "Refresh"
   - Проверьте лог запросов

### Шаг 4: Проверка datasource

Убедитесь, что Grafana подключена к правильной базе:

```bash
# Проверьте настройки datasource
docker-compose exec grafana cat /etc/grafana/provisioning/datasources/postgres.yml
```

Должно быть:
```yaml
datasources:
  - name: LeetCode Tracker PostgreSQL
    type: postgres
    url: db:5432
    database: leetcode_tracker
    user: leetcode_user
```

## 🚀 Деплой на сервер

### Через Git (рекомендуется)

1. Закоммитьте все изменения:
```bash
git add .
git commit -m "Fix Grafana data sync and add diagnostic scripts"
git push origin main
```

2. Подключитесь к серверу и обновите:
```bash
ssh root@v353999.hosted-by-vdsina.com
cd /root/leetcode_tracker_uv
bash deploy.sh
```

### Через SFTP (альтернатива)

Используйте скрипт `deploy.py`:
```bash
uv run deploy
```

## 📊 Настройка фоновой синхронизации

Синхронизация настроена в `leetcode_tracker/config.py`:

```python
# Включить/выключить автосинхронизацию
leetcode_sync_enabled: bool = True

# Интервал синхронизации (секунды)
leetcode_sync_interval: int = 10
```

Измените в `.env` если нужно:
```env
LEETCODE_SYNC_ENABLED=true
LEETCODE_SYNC_INTERVAL=10
```

## 🔍 Отладка

### Проверка логов синхронизации

```bash
# Смотреть логи приложения в реальном времени
docker-compose logs -f app

# Поиск ошибок синхронизации
docker-compose logs app | grep -i "sync\|error"
```

### Проверка базы данных

```bash
# Подключиться к PostgreSQL
docker-compose exec db psql -U leetcode_user -d leetcode_tracker

# В PostgreSQL выполнить:
\dt                          -- Список таблиц
SELECT * FROM users;         -- Все пользователи
SELECT * FROM solved_tasks;  -- Все задачи
```

### Проверка Grafana datasource

```bash
# Логи Grafana
docker-compose logs grafana | grep -i "postgres\|error"

# Перезапустить Grafana
docker-compose restart grafana
```

## ⚠️ Типичные ошибки

### Ошибка: "No data" в Grafana

**Причина**: Пустая таблица `solved_tasks`

**Решение**: 
1. Проверьте, что у пользователя указан `leetcode_username`
2. Дождитесь синхронизации (10 секунд)
3. Или добавьте тестовые данные вручную

### Ошибка: "Connection refused" в Grafana

**Причина**: Grafana не может подключиться к БД

**Решение**:
```bash
# Проверьте, что контейнер БД запущен
docker-compose ps db

# Перезапустите сервисы
docker-compose restart db grafana
```

### Ошибка: Синхронизация не работает

**Причина**: 
- Неверный LeetCode username
- Ошибка API LeetCode
- Отключена автосинхронизация

**Решение**:
```bash
# Проверьте логи
docker-compose logs app | grep -A 10 "sync"

# Проверьте настройку
docker-compose exec app env | grep LEETCODE_SYNC
```

## 📝 Чеклист проверки

- [ ] Контейнеры запущены (`docker-compose ps`)
- [ ] База данных доступна (`pg_isready`)
- [ ] Таблицы созданы (`\dt` в PostgreSQL)
- [ ] Есть пользователи (`SELECT * FROM users`)
- [ ] У пользователя указан `leetcode_username`
- [ ] Автосинхронизация включена (`LEETCODE_SYNC_ENABLED=true`)
- [ ] Логи не показывают ошибки
- [ ] Grafana datasource настроен правильно
- [ ] В Grafana есть данные

## 🎯 Итоговая команда для быстрого фикса

```bash
# 1. Диагностика
bash check_grafana_data.sh

# 2. Настройка синхронизации (введите ваш LeetCode username)
bash test_leetcode_sync.sh

# 3. Проверьте Grafana через 15-20 секунд
# https://novel-cloudtech.com:7443/grafana/
```

## 📚 Дополнительные ресурсы

- [Docker Compose документация](docker-compose.yml)
- [Настройка Grafana](grafana/provisioning/)
- [Модели базы данных](leetcode_tracker/models.py)
- [Фоновая синхронизация](leetcode_tracker/background_sync.py)
- [Конфигурация приложения](leetcode_tracker/config.py)
