# 🔄 Git Workflow для LeetCode Tracker

## Структура веток

### 📌 `main` (основная ветка)
- **Назначение:** Стабильная версия кода
- **Защита:** Прямые коммиты запрещены
- **Обновление:** Только через merge из `deploy` или `features`

### 🚀 `deploy` (production деплой)
- **Назначение:** Готовые к деплою версии
- **Использование:** Автоматический деплой на production сервер
- **Источник:** Merge из `main` после тестирования

### ✨ `features` (разработка)
- **Назначение:** Разработка новых функций и исправлений
- **Использование:** Локальная разработка и тестирование
- **Источник:** Создание feature-веток из `features`

## Workflow процесс

### 1️⃣ Разработка новой функции

```bash
# Переключиться на ветку features
git checkout features

# Создать ветку для новой функции
git checkout -b feature/название-функции

# Разработка и коммиты
git add .
git commit -m "feat: описание изменений"

# Запустить локально для тестирования
./run_local.sh  # или run_local.bat на Windows

# После тестирования - merge в features
git checkout features
git merge feature/название-функции

# Удалить feature-ветку
git branch -d feature/название-функции
```

### 2️⃣ Подготовка к деплою

```bash
# Убедиться что features протестирована
./run_local.sh

# Переключиться на main
git checkout main

# Merge изменений из features
git merge features

# Создать тег версии
git tag -a v2.1.0 -m "Release v2.1.0: описание изменений"

# Push в репозиторий
git push origin main --tags
```

### 3️⃣ Production деплой

```bash
# Переключиться на ветку deploy
git checkout deploy

# Merge из main
git merge main

# Push на сервер (автоматический деплой)
git push origin deploy

# Или использовать скрипт деплоя
./deploy_to_vdsina.sh
```

## Типы коммитов (Conventional Commits)

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации

## Примеры коммитов

```bash
git commit -m "feat: добавлена синхронизация с LeetCode API"
git commit -m "fix: исправлена ошибка 502 в Grafana"
git commit -m "docs: обновлена документация по деплою"
git commit -m "chore: очистка устаревших файлов"
```

## Скрипты для разных окружений

### Локальная разработка (ветка `features`)
```bash
# Linux/Mac
./run_local.sh      # Запуск
./stop_local.sh     # Остановка

# Windows
run_local.bat       # Запуск
stop_local.bat      # Остановка
```

### Production деплой (ветка `deploy`)
```bash
./deploy.sh              # Деплой на сервере
./deploy_to_vdsina.sh    # Деплой с локальной машины
```

## Правила работы

1. ✅ **Всегда тестируй локально** перед merge в `main`
2. ✅ **Используй осмысленные commit messages**
3. ✅ **Создавай feature-ветки** для каждой новой функции
4. ✅ **Обновляй RELEASE_NOTES** при создании новой версии
5. ❌ **Не коммить напрямую в `main`** или `deploy`
6. ❌ **Не пушить неттестированный код** в `deploy`

## Откат изменений

### Откат последнего коммита (локально)
```bash
git reset --soft HEAD~1  # Сохранить изменения
git reset --hard HEAD~1  # Удалить изменения
```

### Откат деплоя на production
```bash
# На сервере
git checkout deploy
git reset --hard HEAD~1
./deploy.sh
```

## Полезные команды

```bash
# Посмотреть статус
git status

# Посмотреть историю
git log --oneline --graph --all

# Посмотреть изменения
git diff

# Посмотреть все ветки
git branch -a

# Удалить локальную ветку
git branch -d название-ветки

# Синхронизация с удаленным репозиторием
git fetch origin
git pull origin main
```
