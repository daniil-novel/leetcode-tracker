# Релиз v2.0.0 - Исправления Production Развертывания и Интеграция Grafana

## 🎉 Мажорный Релиз - Полное Production Развертывание

Этот релиз включает критические исправления для production развертывания и полную интеграцию мониторинга Grafana.

---

## 🚀 Новые Возможности

### Дашборд Мониторинга Grafana
- ✅ **Полная интеграция Grafana** с автоматическим provisioning
- ✅ **8 панелей мониторинга**:
  - Всего Решенных Задач
  - Общий XP
  - Активные Пользователи
  - Среднее Время на Задачу
  - График Решенных Задач по Времени (time series)
  - Задачи по Сложности (круговая диаграмма)
  - Таблица Лидеров
  - Последние Задачи (таблица)
- ✅ **Автообновление каждые 5 секунд**
- ✅ **PostgreSQL datasource** предварительно настроен
- ✅ **Доступен по адресу** `https://novel-cloudtech.com:7443/grafana/`

### Health Check Endpoint
- ✅ Добавлен `/health` endpoint для Docker healthcheck
- ✅ Улучшена надежность и мониторинг контейнеров

---

## 🐛 Критические Исправления Ошибок

### Проблемы Docker Развертывания

#### 1. Исправлена ошибка `KeyError: 'ContainerConfig'`
**Проблема**: Старая версия docker-compose (1.29.2) не могла пересоздать контейнеры из-за несовместимых метаданных.

**Решение**:
- Улучшен `deploy.sh` с агрессивной очисткой контейнеров
- Добавлен `docker-compose down --remove-orphans` перед развертыванием
- Принудительное удаление зомби-контейнеров по именам
- Добавлена задержка 2 секунды для освобождения портов

#### 2. Исправлены Конфликты Портов
**Проблема**: Порты 9091 и 9092 были заняты внешними процессами (Node.js и outline-ss-ser).

**Решение**:
- Изменен порт Prometheus с 9091 → 9093
- Обновлен `docker-compose.yml` с новым маппингом портов
- Созданы диагностические скрипты (`cleanup_docker.sh`, `check_ports.sh`)

#### 3. Исправлено Несоответствие Версий PostgreSQL
**Проблема**: Volume базы данных был создан с PostgreSQL 16, но docker-compose пытался использовать PostgreSQL 15.

**Ошибка**:
```
FATAL: database files are incompatible with server
The data directory was initialized by PostgreSQL version 16, 
which is not compatible with this version 15.15
```

**Решение**:
- Обновлен `docker-compose.yml`: `postgres:15-alpine` → `postgres:16-alpine`
- База данных теперь запускается успешно

#### 4. Исправлена Маршрутизация Grafana Subpath
**Проблема**: Grafana была недоступна по пути `/grafana/` - FastAPI перехватывал маршрут.

**Решение**:
- Добавлены переменные окружения Grafana:
  - `GF_SERVER_ROOT_URL=https://novel-cloudtech.com:7443/grafana/`
  - `GF_SERVER_SERVE_FROM_SUB_PATH=true`
- Обновлена конфигурация nginx с правильными rewrite правилами
- Исключен `/grafana` из catch-all маршрута FastAPI

---

## 📝 Изменения Конфигурации

### docker-compose.yml
```yaml
# PostgreSQL обновлен до v16
db:
  image: postgres:16-alpine

# Изменен порт Prometheus
prometheus:
  ports:
    - "9093:9090"  # Изменено с 9091

# Конфигурация Grafana subpath
grafana:
  environment:
    - GF_SERVER_ROOT_URL=https://novel-cloudtech.com:7443/grafana/
    - GF_SERVER_SERVE_FROM_SUB_PATH=true

# Добавлен healthcheck для app
app:
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
    interval: 10s
    timeout: 5s
    retries: 5
    start_period: 30s
```

### nginx-leetcode-tracker.conf
```nginx
# Proxy Grafana с rewrite
location /grafana/ {
    proxy_pass http://127.0.0.1:3000/;
    rewrite ^/grafana/(.*) /$1 break;
    # ... другие настройки proxy
}
```

### leetcode_tracker/main.py
```python
# Health check endpoint
@app.get("/health")
async def health_check():
    return {"status": "healthy"}

# Исключение Grafana из SPA роутинга
if full_path.startswith(("api/", "auth/", "add/", "stats/", "static/", "grafana/", "grafana")):
    raise HTTPException(status_code=404, detail="Not found")
```

---

## 🛠️ Новые Утилиты

### cleanup_docker.sh
Скрипт аварийной очистки Docker контейнеров и портов:
- Останавливает все контейнеры проекта
- Принудительно удаляет контейнеры по именам
- Проверяет доступность портов
- Удаляет висячие контейнеры

### check_containers.sh
Диагностический скрипт для проверки статуса контейнеров и логов.

### diagnose_app.sh
Инструмент диагностики приложения для устранения проблем с unhealthy контейнерами.

### fix_db_issue.sh
Скрипт устранения неполадок и восстановления базы данных.

---

## 📚 Новая Документация

### FINAL_DEPLOYMENT_INSTRUCTIONS.md
Полное пошаговое руководство по развертыванию с:
- Описанием проблемы
- Обзором решения
- Детальными командами для развертывания на сервере
- Шагами по устранению неполадок

### DEPLOYMENT_TROUBLESHOOTING.md
Комплексное руководство по устранению неполадок, охватывающее:
- Ошибки ContainerConfig
- Конфликты портов
- Unhealthy контейнеры
- Проблемы с базой данных
- Полезные команды и советы

---

## 🔧 Инструкции по Развертыванию

### Быстрое Развертывание
```bash
cd ~/leetcode_tracker_uv
git pull
./deploy.sh
```

### Ручное Развертывание
```bash
# 1. Очистка старых контейнеров
docker ps -a --filter "name=prometheus" -q | xargs -r docker rm -f
docker ps -a --filter "name=leetcode" -q | xargs -r docker rm -f

# 2. Развертывание
git pull
chmod +x deploy.sh
./deploy.sh
```

---

## 🌐 Точки Доступа

- **Приложение**: https://novel-cloudtech.com:7443/
- **Grafana**: https://novel-cloudtech.com:7443/grafana/
  - Логин: `admin`
  - Пароль: `admin`
- **Prometheus**: http://<server-ip>:9093
- **cAdvisor**: http://<server-ip>:8081

---

## ⚙️ Системные Требования

- Docker Engine 20.10+
- Docker Compose 1.29+ (или Docker Compose V2)
- PostgreSQL 16
- Рекомендуется 2GB+ RAM
- Доступные порты: 8000, 3000, 9093, 8081

---

## 🔄 Примечания по Миграции

При обновлении с v1.x:

1. **База данных**: Volume будет сохранен (совместим с PostgreSQL 16)
2. **Контейнеры**: Все контейнеры будут пересозданы
3. **Конфигурация**: Проверьте файл `.env` на наличие новых переменных
4. **Nginx**: Обновите конфигурацию nginx при использовании кастомной настройки

---

## 🐞 Известные Проблемы

- Старая версия docker-compose (1.29.2) может все еще вызывать проблемы - рекомендуется обновление до Docker Compose V2
- Порты 9091 и 9092 должны оставаться свободными (заняты внешними сервисами на некоторых серверах)

---

## 👥 Участники

- @daniil-novel - Полные исправления развертывания и интеграция Grafana

---

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей

---

## 🙏 Благодарности

Особая благодарность сообществу за сообщения о проблемах развертывания и помощь в тестировании!
